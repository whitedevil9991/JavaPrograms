public class DemoLockInterface {
    /**1) Lock interface
     * 2)ReentrantLock = used to create lock
     * when you use synchronized we lock it but OS unlocks it
     * if not using sync, we have to call lockMethod and unlockMethod
     */
    
}
